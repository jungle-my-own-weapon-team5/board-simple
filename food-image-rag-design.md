# 음식 이미지 RAG 검색기 설계

## 목적

이 설계의 목적은 사용자가 음식 이미지를 입력했을 때 매번 LLM에게 이미지를 직접 판단시키지 않고, 기존에 저장된 이미지 벡터와의 유사도를 먼저 확인해서 LLM 호출 비용을 줄이는 것이다.

즉, 이 시스템은 일반적인 음식 분류기라기보다 **LLM 호출 절감을 위한 이미지 embedding 기반 semantic cache / 검색기**에 가깝다.

## 기본 흐름

1. 음식 이미지가 입력된다.
2. ImageNet으로 학습된 ResNet 계열 모델을 사용해 이미지 embedding을 생성한다.
3. 생성된 embedding과 기존 DB에 저장된 음식 이미지 embedding들의 유사도를 계산한다.
4. 유사도가 충분히 높으면 LLM을 호출하지 않고 기존 검색 결과를 그대로 반환한다.
5. 유사도가 애매하거나 낮으면 LLM에게 이미지를 판단시킨다.
6. LLM 판단 결과를 음식명, 설명, embedding, 메타데이터와 함께 저장한다.
7. 이후 비슷한 이미지가 들어오면 저장된 결과를 재사용한다.
8. 데이터가 충분히 쌓이면 마지막 layer 또는 projection layer를 음식 데이터에 맞게 학습한다.

## 유사도 기준

처음 구상한 기준은 다음과 같다.

| 유사도 구간 | 처리 방식 |
|---|---|
| 90% 이상 | LLM 호출 없이 기존 검색 결과 반환 |
| 50% 이상 90% 미만 | LLM에게 음식 판단 요청 |
| 50% 미만 | LLM 호출 또는 미확인 데이터로 처리 |

단, 이 threshold는 고정값으로 바로 정하기보다 실제 음식 이미지 데이터에서 같은 음식과 다른 음식의 유사도 분포를 확인한 뒤 조정하는 것이 좋다.

## Top-K 검색 방식

가장 가까운 이미지 1개만 보고 결과를 결정하면 우연히 잘못된 이미지에 매칭될 수 있다.

따라서 음식 이미지 검색에서는 Top-K 검색을 사용하는 것이 더 안정적이다.

예를 들어 새 이미지와 가장 가까운 5개 결과가 다음과 같다면:

| Top-K 결과 | 판단 |
|---|---|
| 김치찌개, 김치찌개, 김치찌개, 된장찌개, 순두부찌개 | 김치찌개일 가능성이 높음 |
| 김치찌개, 라멘, 마라탕, 토마토스프, 순두부찌개 | 애매하므로 LLM 호출 필요 |

즉, 최고 유사도 하나만 보는 것보다 Top-K 결과의 라벨 일관성을 함께 보는 것이 좋다.

## 유사도 함수

사용자가 생각한 식은 각 차원의 차이에 대해 다음 값을 계산하는 형태다.

```text
e^-(xi - xj)^2
```

이를 모든 차원에 대해 곱하면 다음과 비슷한 형태가 된다.

```text
exp(-Σ(xi - xj)^2)
```

이것은 RBF kernel 또는 Gaussian similarity와 비슷하다.

다만 embedding 차원이 512 또는 1578처럼 커지면 모든 차원의 값을 곱하는 방식은 값이 너무 빠르게 0에 가까워질 수 있다. 예를 들어 각 차원의 유사도가 평균적으로 0.99여도 512차원에서 곱하면 매우 작은 값이 된다.

따라서 초기 구현에서는 다음 방식 중 하나를 사용하는 것이 더 현실적이다.

| 방식 | 특징 |
|---|---|
| cosine similarity | embedding 검색에서 가장 흔히 사용 |
| L2 distance | 이미지 feature 거리 비교에 사용 가능 |
| RBF kernel | 가능하지만 스케일 조절이 필요 |

검색 목적에서는 유사도 값이 반드시 확률일 필요는 없다. 중요한 것은 기존 결과를 가까운 순서로 정렬할 수 있는 점수다.

## ResNet 입력 크기

ImageNet으로 학습된 ResNet-18 또는 ResNet-34는 보통 `224 x 224` 입력을 기준으로 사용한다.

음식 사진은 문서 이미지처럼 작은 글자나 표를 보존해야 하는 경우가 적기 때문에, 일반적으로 `1920 x 1080` 이미지를 `224 x 224` 근처로 줄여도 사용할 수 있다.

다만 이미지를 정사각형으로 강제로 찌그러뜨리기보다는 다음 중 하나가 더 적절하다.

| 방식 | 특징 |
|---|---|
| resize 후 center crop | 음식이 중앙에 크게 있을 때 적절 |
| 비율 유지 resize 후 padding | 음식이 가장자리에 있거나 여러 개 있을 때 안전 |

## ResNet-18과 ResNet-34 선택

음식 이미지 검색 목적이라면 ResNet-18부터 시작해도 된다.

| 모델 | 특징 |
|---|---|
| ResNet-18 | 빠르고 가벼움, 작은 프로젝트나 초기 검증에 적합 |
| ResNet-34 | ResNet-18보다 깊고 표현력이 좋음, 비용은 조금 더 큼 |

둘 다 마지막 convolution 이후 global average pooling을 거치면 보통 `512차원 feature`를 얻을 수 있다.

검색기 초기 버전에서는 마지막 FC layer의 분류 결과보다 `512차원 feature`를 embedding으로 사용하는 것이 적절하다.

## GPT embedding 차원과 ResNet embedding 차원

GPT 텍스트 embedding이 특정 차원이라고 해서 ResNet 이미지 embedding을 같은 차원으로 맞추면 바로 비교할 수 있는 것은 아니다.

차원 수가 같아도 두 embedding이 학습된 의미 공간이 다르면 직접 비교할 수 없다.

| embedding | 의미 |
|---|---|
| GPT 텍스트 embedding | 문장과 단어의 의미 관계 |
| ResNet 이미지 embedding | 색, 형태, 질감, 물체 패턴 |

따라서 ResNet의 `512차원` feature를 단순히 `1578차원`으로 늘린다고 해서 GPT embedding 공간과 같은 의미를 갖지는 않는다.

텍스트와 이미지를 같은 공간에서 비교해야 한다면 CLIP 같은 vision-language embedding 모델이 더 자연스럽다.

하지만 현재 목적이 LLM 호출 절감을 위한 음식 이미지 검색이라면, 처음에는 ResNet의 이미지 embedding만으로 충분히 시작할 수 있다.

## 마지막 layer만 학습한다는 의미

ImageNet으로 학습된 ResNet의 앞부분은 이미 일반적인 시각 특성을 잘 추출한다.

예를 들어 다음과 같은 특성을 학습해두었다.

- 선
- 색 대비
- 모서리
- 질감
- 둥근 형태
- 반복 패턴
- 물체 일부의 조합

음식 사진에서도 국물 색, 면의 질감, 튀김 표면, 접시 경계 같은 특성은 여전히 유용하다.

따라서 초기에는 ResNet 전체를 다시 학습하지 않고, 앞부분 feature extractor는 고정한 뒤 마지막 판단 layer만 음식 데이터에 맞게 학습하는 것이 좋다.

구조는 다음과 같다.

```text
음식 이미지
-> ResNet feature extractor
-> 512차원 feature
-> classifier 또는 projection layer
-> 음식 판단 또는 검색용 embedding
```

고정하는 부분:

```text
ResNet feature extractor
```

학습하는 부분:

```text
마지막 classifier layer 또는 projection layer
```

이 방식은 전체 ResNet을 fine-tuning하는 것보다 비용이 적고, 데이터가 적을 때 기존 ImageNet feature가 망가질 위험도 줄일 수 있다.

## 학습 단계

처음부터 전체 모델을 다시 학습하는 것은 권장하지 않는다.

단계적으로 접근하는 것이 좋다.

| 단계 | 방식 | 설명 |
|---|---|---|
| 1단계 | 학습 없음 | pretrained ResNet embedding으로 검색만 수행 |
| 2단계 | 마지막 layer만 학습 | ResNet feature를 음식 라벨 또는 검색 embedding에 맞게 조정 |
| 3단계 | 뒤쪽 block 일부 fine-tuning | 데이터가 충분히 쌓였을 때 적용 |
| 4단계 | 전체 fine-tuning | 검증셋과 데이터가 충분할 때만 고려 |

현재 목적에서는 1단계와 2단계가 가장 현실적이다.

## 저장해야 할 데이터

LLM 호출 결과를 재사용하려면 embedding뿐 아니라 메타데이터도 함께 저장하는 것이 좋다.

| 데이터 | 설명 |
|---|---|
| image_embedding | ResNet 또는 다른 이미지 모델로 생성한 벡터 |
| food_name | LLM이 판단한 음식명 |
| llm_response | LLM이 생성한 원래 응답 |
| confidence | LLM 판단 신뢰도 또는 내부 기준 점수 |
| source_image_id | 원본 이미지 식별자 |
| created_at | 생성 시점 |
| hit_count | 재사용된 횟수 |
| feedback | 사용자의 정정 또는 확인 결과 |

## 우선 검증할 질문

이 설계에서 가장 먼저 확인해야 할 것은 threshold가 실제로 의미 있는지다.

예를 들어 다음 질문을 작은 데이터셋으로 확인해야 한다.

- 같은 음식 사진끼리 cosine similarity가 보통 어느 정도 나오는가?
- 다른 음식 사진끼리도 높은 유사도가 나오는 경우가 있는가?
- 유사도 90% 이상이면 정말 같은 음식으로 봐도 되는가?
- Top-1보다 Top-K 라벨 투표가 더 안정적인가?
- LLM 호출을 얼마나 줄일 수 있는가?
- LLM 판단 오류가 캐시에 누적될 때 어느 정도까지 허용 가능한가?

## 현재 설계 방향

현재 목적에 가장 맞는 초기 설계는 다음과 같다.

1. ImageNet pretrained ResNet-18 또는 ResNet-34를 사용한다.
2. 마지막 FC layer 결과가 아니라 global average pooling 이후의 `512차원 feature`를 embedding으로 사용한다.
3. 기존 음식 이미지 embedding과 cosine similarity로 Top-K 검색한다.
4. Top-K 결과의 유사도와 라벨 일관성이 충분하면 기존 검색 결과를 반환한다.
5. 애매하면 LLM을 호출한다.
6. LLM 판단 결과를 embedding과 함께 저장한다.
7. 데이터가 충분히 쌓이면 ResNet 전체가 아니라 마지막 classifier 또는 projection layer부터 학습한다.



| 상황                        |                  이미지 입력 토큰 |
| ------------------------- | -------------------------: |
| `detail: low`             |             모델별 고정 base 토큰 |
| GPT-4o / GPT-4.1 계열 `low` |                 약 **85토큰** |
| GPT-5 계열 `low`            |                 약 **70토큰** |
| `detail: high`            | base 토큰 + 512px 타일마다 추가 토큰 |

그래서 전체 사용량은 보통:

입력 기준: 약 80~100토큰대
입출력 합산: 약 100~200토큰 정도

| 모델          | 입력 100만 토큰 | 출력 100만 토큰 |     입력 1토큰당 |    출력 1토큰당 |
| ----------- | ---------: | ---------: | ----------: | ---------: |
| GPT-5 nano  |      $0.05 |      $0.40 | $0.00000005 | $0.0000004 |
| GPT-5 mini  |      $0.25 |      $2.00 | $0.00000025 |  $0.000002 |
| GPT-5       |      $1.25 |     $10.00 | $0.00000125 |   $0.00001 |
| GPT-5.5     |      $2.50 |     $15.00 |  $0.0000025 |  $0.000015 |
| GPT-5.5 pro |     $15.00 |     $90.00 |   $0.000015 |   $0.00009 |

-> 해당 기술을 쓰면 최신 모델을 써도 일정한 비용으로 아끼는 것이 가능
